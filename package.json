{
  "name": "codingrankproject",
  "version": "1.0.0",
  "description": "MERN-style coding leaderboard with React, Tailwind CSS, Express, LeetCode, and Codeforces integrations.",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "start": "npm start --prefix backend",
    "build": "npm run build --prefix frontend"
  },
  "keywords": [
    "mern",
    "leaderboard",
    "leetcode",
    "codeforces"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
